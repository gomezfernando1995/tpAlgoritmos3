import { CambiarCursorDirective } from './cambiar-cursor.directive';

describe('CambiarCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new CambiarCursorDirective();
    expect(directive).toBeTruthy();
  });
});
